# Codex app-server SDK dependency
#
# The default backend imports this Python module:
#
#   from codex_app_server import AppServerConfig, Codex
#
# Public PyPI does not currently expose a distribution named `codex_app_server`.
# Install the SDK from your approved private index or direct URL before running
# `--backend=codex_app_server`, for example:
#
#   pip install "${CODEX_APP_SERVER_PACKAGE}"
#
# where CODEX_APP_SERVER_PACKAGE is a pinned requirement from your release
# process, such as a private-index package/version or a direct URL.
